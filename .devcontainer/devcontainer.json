{
    "name": "Claude Code Sandbox",
    "build": { "dockerfile": "Dockerfile" },
    "remoteUser": "developer",
    "updateRemoteUserUID": true,
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "runArgs": [
        "--userns=keep-id"
    ],
    "containerEnv": {
        "TZ": "America/Los_Angeles",
        "TERM": "xterm-256color"
    },
    "mounts": [
        "source=${localEnv:HOME}/.claude,target=/home/developer/.claude,type=bind,z",
        "source=${localEnv:HOME}/.claude.json,target=/home/developer/.claude.json,type=bind,z",
        "source=${localEnv:HOME}/.local/bin/claude,target=/home/developer/.local/bin/claude,type=bind,z",
        "source=${localEnv:HOME}/.local/bin/xclaude,target=/home/developer/.local/bin/xclaude,type=bind,z",
        "source=${localEnv:HOME}/.local/share/claude,target=/home/developer/.local/share/claude,type=bind,z"
    ]
}
